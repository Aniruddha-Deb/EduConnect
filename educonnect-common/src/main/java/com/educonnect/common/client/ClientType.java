package com.educonnect.common.client;

public enum ClientType {

	/**
	 * Used to identify an administrator logging on to the system
	 */
	ADMIN, 
	
	/**
	 * Used to identify a student logging on to the system
	 */
	STUDENT;
}
